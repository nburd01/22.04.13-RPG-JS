class Turn {
    constructor(players, attack, target) {
      this.player = player;
      this.attack = attack;
      this.target = target;
    }


    attack(){
        // si l'action correspond à une attaque 
      if (this.attack == attack) 
        //   et si la cible a des hp supérieur à 0
        if (target hp > 0)
        //alors le joueur fait des dommages à la cible
        this.player make dmg à this.target
        // et si target a zero on fait un console log
        (if target <= 0)
        console.log(`le joueur ${this.target} est mort !`)

    }


}

// raaaaah putaaaaain 